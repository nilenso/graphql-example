{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "server": "yarn --cwd backend start",
    "client": "yarn --cwd frontend start",
    "dev-server": "yarn --cwd backend dev",
    "frontend-tests": "yarn --cwd frontend test"
  }
}
